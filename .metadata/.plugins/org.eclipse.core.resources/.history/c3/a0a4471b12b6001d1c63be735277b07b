package exercicos;

import java.util.Scanner;

public class Emprestimo {

	public static void main(String[] args) {
		Scanner teclado = new Scanner(System.in);
		
		System.out.print("Valor do emprestimo: ");	
		double valorEmprestimo = teclado.nextDouble();
		
		System.out.print("Quantidade das parcelas: ");
		int quantidadeParcelas = teclado.nextInt();
		
		parcelar(valorEmprestimo, quantidadeParcelas);
		
		teclado.close();
				
	}
	
	public static void taxar(double emprestimo, int valor) {
		if(valor <= 100) {
			int valorPagamento = (int) ((emprestimo * 10 / 100) + emprestimo);
			System.out.println(valorPagamento);
		}
	}
	
	public static void parcelar(double emprestimo, int valor) {
		if(emprestimo <= 100) {
			taxar(emprestimo, valor);
		//	System.out.println();
		}
	}

}
